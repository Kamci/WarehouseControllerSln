<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WarehouseController.OrderPage"
             xmlns:conventers="clr-namespace:WarehouseController.Converters"  
             xmlns:local="clr-namespace:WarehouseController.ViewModel.OrderVM;assembly=WarehouseController"  
             xmlns:model="clr-namespace:WarehouseController.DTO"  
             x:DataType="local:OrderViewModel"  
             Title="{Binding Title}">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>


        </Grid.RowDefinitions>
        <!-- 🧰 Nasz custom toolbar -->
        <Grid ColumnSpacing="{OnPlatform Android=40, WinUI=0 }"  
            Grid.Row="0"
          VerticalOptions="Center"  
          HorizontalOptions="Center"  
          Padding="{OnPlatform Android=40, WinUI=0 }"  
          BackgroundColor="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ImageButton Grid.Column="0"  
                     Source="add.png"  
                     Command="{Binding AddItemCommand}"/>
            <ImageButton  Grid.Column="1"  
                     Source="refresh.png"  
                     Command="{Binding LoadItemsCommand}"/>
            <ImageButton  Grid.Column="2"  
                     Source="about.png"  
                     Command="{Binding AboutCommand}"  
                     CommandParameter="{Binding SelectedOrder}"  
                     IsEnabled="{Binding SelectedOrder, Converter={StaticResource NullToBoolConventer}}"/>
        </Grid>
        <Grid  Grid.Row="1">
            <ScrollView>
                <VerticalStackLayout>
                    <Label HorizontalOptions="Center" Text="Available Orders" FontSize="20"   
               FontAttributes="Bold" Margin="20" />

            <CollectionView x:Name="OrdersList" ItemsSource="{Binding Items}"  
                            SelectionMode="Single" SelectedItem="{Binding SelectedOrder}"   
                            SelectionChanged="OrdersList_SelectionChanged">
                <CollectionView.ItemTemplate >
                    <DataTemplate x:DataType="model:OrderDto">
                        <Grid Padding="10" BackgroundColor="{StaticResource Primary} ">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup Name="CommonStates">
                                    <VisualState Name="Normal">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="DarkGreen" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Grid.ColumnDefinitions >
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Label Text="Id:" FontSize="18" FontAttributes="Bold" Grid.Row="0" Grid.Column="0" Margin="0,0,10,5"  />
                            <Label Text="{Binding Id}" FontSize="18" FontAttributes="Bold" Grid.Row="0" Grid.Column="1"/>
                            <Label Text="Order Date: " FontSize="14" Grid.Row="1" Grid.Column="0" Margin="0,0,10,5"  />
                            <Label Text="{Binding OrderDate, StringFormat='{}{0:yyyy-MM-dd}'}"  FontSize="14" Grid.Row="1" Grid.Column="1" />
                            <Label Text="Status: " FontSize="14" Grid.Row="2" Grid.Column="0" Margin="0,0,10,5" />
                            <Label Text="{Binding Status}" FontSize="14" Grid.Row="2" Grid.Column="1" />
                            <Label Text="User Id: " FontSize="14" Grid.Row="3" Grid.Column="0" Margin="0,0,10,5" />
                            <Label Text="{Binding UserLogin}" FontSize="14" Grid.Row="3" Grid.Column="1" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
                </VerticalStackLayout>
            </ScrollView>
        </Grid>
    </Grid>
</ContentPage>